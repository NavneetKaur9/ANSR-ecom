Hackerearth challenge - ANSR E-commerce

Backend:
### Install and run the app

`npm install && npm start`

Frontend:
### Install and run the app

open another terminal

`cd frontend && npm install && npm start`

